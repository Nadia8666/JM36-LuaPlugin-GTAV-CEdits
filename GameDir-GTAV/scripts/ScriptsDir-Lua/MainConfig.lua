-- Configuration system test, removes the need to restart the game for main config changes
local MainConfig = {

    DebugMode = true, -- Enable script reloading with the script reload key (default TAB), Usage: DebugMode = value,

}



return MainConfig